name: Data Engineering Prod Ingestion

on:
  workflow_dispatch:
    inputs:
      source:
        description: "Source a ingerer en base prod"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - service_public
          - legifrance
      wipe_existing_chunks:
        description: "Supprimer les chunks Service-Public existants avant re-ingestion (requiert run_embeddings=true)"
        required: true
        default: false
        type: boolean
      run_embeddings:
        description: "Backfill embeddings Service-Public apres un wipe de chunks"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: data-prod-ingestion
  cancel-in-progress: false

jobs:
  ingest-prod:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    environment: scaleway-production
    env:
      UV_CACHE_DIR: .uv-cache
      SCW_DEFAULT_REGION: fr-par
      SCW_BUCKET_BRONZE: assistant-rh-bronze
      SCW_BUCKET_SILVER: assistant-rh-silver
      SCW_BUCKET_GOLD: assistant-rh-gold
      SCW_PREFIX_STAGING: staging
      SCW_PREFIX_PROD: prod
      SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
      SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
      SCW_POSTGRES_DSN: ${{ secrets.SCW_POSTGRES_DSN }}
      SCALEWAY_API_KEY: ${{ secrets.SCALEWAY_API_KEY }}
      SCALEWAY_BASE_URL: ${{ vars.SCALEWAY_BASE_URL || 'https://api.scaleway.ai/v1' }}
      WIPE_EXISTING_CHUNKS: ${{ github.event.inputs.wipe_existing_chunks || 'false' }}
      RUN_EMBEDDINGS: ${{ github.event.inputs.run_embeddings || 'false' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Validate Service-Public wipe/backfill selection
        if: ${{ github.event.inputs.source == 'all' || github.event.inputs.source == 'service_public' }}
        run: |
          if [[ "${WIPE_EXISTING_CHUNKS}" == "true" && "${RUN_EMBEDDINGS}" != "true" ]]; then
            echo "::error::wipe_existing_chunks deletes Service-Public chunk embeddings. Select run_embeddings=true so the freshly inserted chunks are backfilled."
            exit 1
          fi

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --group dev --frozen

      - name: Ingest Service-Public into Scaleway prod
        if: ${{ github.event.inputs.source == 'all' || github.event.inputs.source == 'service_public' }}
        run: |
          WIPE_ARGS=()
          if [[ "${WIPE_EXISTING_CHUNKS}" == "true" ]]; then
            WIPE_ARGS+=(--wipe-existing-chunks)
          fi

          uv run --no-sync data-ingestion service-public ingest \
            --fiche-config config/service_public_fiches.json \
            --target-env prod \
            --from-object-storage \
            --dsn "${SCW_POSTGRES_DSN}" \
            "${WIPE_ARGS[@]}"

      - name: Backfill Service-Public embeddings after chunk wipe
        if: ${{ (github.event.inputs.source == 'all' || github.event.inputs.source == 'service_public') && inputs.wipe_existing_chunks && inputs.run_embeddings }}
        run: |
          uv run --no-sync data-ingestion embeddings service-public \
            --dsn-env SCW_POSTGRES_DSN

      - name: Ingest Legifrance into Scaleway prod
        if: ${{ github.event.inputs.source == 'all' || github.event.inputs.source == 'legifrance' }}
        run: |
          uv run --no-sync data-ingestion legifrance ingest \
            --target-env prod \
            --from-object-storage \
            --load-all-artifacts \
            --dsn "${SCW_POSTGRES_DSN}"
