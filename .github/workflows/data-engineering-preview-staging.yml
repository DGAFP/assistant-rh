name: Data Engineering Preview Staging

on:
  push:
    branches:
      - staging
    paths:
      - ".github/workflows/data-engineering-preview-staging.yml"
      - ".github/workflows/data-engineering-cron-delta.yml"
      - ".github/scripts/data_engineering_plan.py"
      - ".github/scripts/scaleway_data_jobs.py"
      - ".github/data-engineering-jobs.json"
      - "apps/data-ingestion-cli/**"
      - "packages/data-engineering/**"
      - "packages/shared-config/**"
      - "config/**"
      - "Dockerfile.service_public_pipeline"
      - "Dockerfile.service_public_ingestion"
      - "Dockerfile.legifrance_pipeline"
      - "Dockerfile.legifrance_ingestion"
      - "Dockerfile.legifrance_bulk_dump"
      - "Dockerfile.embeddings_job"
      - "Dockerfile.pdf_sources_pipeline"
  pull_request:
    branches:
      - staging
    paths:
      - ".github/workflows/data-engineering-preview-staging.yml"
      - ".github/workflows/data-engineering-cron-delta.yml"
      - ".github/scripts/data_engineering_plan.py"
      - ".github/scripts/scaleway_data_jobs.py"
      - ".github/data-engineering-jobs.json"
      - "apps/data-ingestion-cli/**"
      - "packages/data-engineering/**"
      - "packages/shared-config/**"
      - "config/**"
      - "Dockerfile.service_public_pipeline"
      - "Dockerfile.service_public_ingestion"
      - "Dockerfile.legifrance_pipeline"
      - "Dockerfile.legifrance_ingestion"
      - "Dockerfile.legifrance_bulk_dump"
      - "Dockerfile.embeddings_job"
      - "Dockerfile.pdf_sources_pipeline"
  workflow_dispatch:
    inputs:
      source:
        description: "Source a traiter"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - service_public
          - legifrance
          - matte
          - mi
          - masa
          - mso
          - pdf_sources
          - embeddings
          - r2
      mode:
        description: "plan/apply pour les ingestions ; generate est réservé à source=r2"
        required: true
        default: "apply"
        type: choice
        options:
          - apply
          - plan
          - generate
      r2_reviewed_cache:
        description: "Confirmation obligatoire avant apply R2 : le cache généré a été revu"
        required: true
        default: false
        type: boolean
      r2_cache_source_env:
        description: "Cache Gold à hydrater avant la cible (staging pour promouvoir vers prod)"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - prod
      r2_limit:
        description: "Limiter le nombre d'articles R2 (0 = tout le delta)"
        required: true
        default: 0
        type: number
      r2_allow_cache_misses:
        description: "Apply R2 partiel malgré des caches manquants"
        required: true
        default: false
        type: boolean
      delta:
        description: "Reconciliation incrementale canonique (Service-Public via Grist, Legifrance via Grist + PISTE)"
        required: true
        default: true
        type: boolean
      run_ingestion:
        description: "Ingerer aussi dans la base Scaleway staging"
        required: true
        default: true
        type: boolean
      wipe_existing_chunks:
        description: "Supprimer les chunks Service-Public existants avant re-ingestion (requiert run_embeddings=true et embedding_source=service_public/all)"
        required: true
        default: false
        type: boolean
      run_embeddings:
        description: "Backfill embeddings si l'image embeddings est concernee (obligatoire avec wipe_existing_chunks)"
        required: true
        default: false
        type: boolean
      embedding_source:
        description: "Source embeddings a backfiller"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - service_public
          - legifrance
          - matte
          - mi
          - masa
          - mso
      embedding_only_column:
        description: "Colonne embeddings cible optionnelle, ex: embedding_m3"
        required: false
        default: ""
        type: string
      run_preview_jobs:
        description: "Executer les jobs Scaleway staging (medallion/ingestion/embeddings)"
        required: true
        default: false
        type: boolean
      service_public_fiche_config:
        description: "Config JSON Service-Public"
        required: true
        default: "config/service_public_fiches.json"
        type: string
      legifrance_article_ids_json:
        description: "Manifest article IDs Legifrance pour le bulk dump"
        required: true
        default: "config/legifrance_article_cids.json"
        type: string

permissions:
  contents: read

concurrency:
  group: data-preview-staging-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY_HOST: rg.fr-par.scw.cloud
  STAGING_IMAGE_TAG: staging-${{ github.sha }}

jobs:
  plan:
    if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'release-please--') }}
    runs-on: ubuntu-latest
    outputs:
      service_public: ${{ steps.plan.outputs.service_public }}
      legifrance: ${{ steps.plan.outputs.legifrance }}
      pdf_sources: ${{ steps.plan.outputs.pdf_sources }}
      # Sans cette déclaration, needs.plan.outputs.pdf_sources_ministry (passé à
      # scaleway_data_jobs.py) se résout en chaîne vide et un dispatch
      # source=<ministère> lance le domaine pdf_sources ENTIER (constaté le
      # 05/07: source=matte a démarré mi+masa+matte).
      pdf_sources_ministry: ${{ steps.plan.outputs.pdf_sources_ministry }}
      embeddings: ${{ steps.plan.outputs.embeddings }}
      r2: ${{ steps.plan.outputs.r2 }}
      run_embeddings: ${{ steps.plan.outputs.run_embeddings }}
      embedding_source: ${{ steps.plan.outputs.embedding_source }}
      has_builds: ${{ steps.plan.outputs.has_builds }}
      has_runs: ${{ steps.plan.outputs.has_runs }}
      matrix: ${{ steps.plan.outputs.matrix }}
      run_matrix: ${{ steps.plan.outputs.run_matrix }}
      mode: ${{ steps.plan.outputs.mode }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Detect changed data engineering jobs
        id: plan
        env:
          GITHUB_EVENT_BEFORE: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          INPUT_SOURCE: ${{ github.event_name == 'workflow_dispatch' && inputs.source || '' }}
          INPUT_RUN_EMBEDDINGS: ${{ github.event_name == 'workflow_dispatch' && inputs.run_embeddings || false }}
          INPUT_EMBEDDING_SOURCE: ${{ github.event_name == 'workflow_dispatch' && inputs.embedding_source || '' }}
          INPUT_MODE: ${{ github.event_name == 'workflow_dispatch' && inputs.mode || 'apply' }}
        run: python3 .github/scripts/data_engineering_plan.py

  build-staging-images:
    runs-on: ubuntu-latest
    needs: plan
    # Pull-request refs are intentionally not allowed by the scaleway-staging
    # environment. PRs keep the secretless plan job; image builds and previews
    # start only from an integrated staging ref or an approved manual dispatch.
    if: ${{ github.event_name != 'pull_request' && needs.plan.result == 'success' && (needs.plan.outputs.mode == 'apply' || needs.plan.outputs.r2 == 'true') && needs.plan.outputs.has_builds == 'true' }}
    timeout-minutes: 90
    environment: scaleway-staging
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.matrix) }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Scaleway Container Registry
        env:
          REGISTRY_NAMESPACE: ${{ vars.SCW_CONTAINER_REGISTRY_NAMESPACE }}
          REGISTRY_PASSWORD: ${{ secrets.SCW_SECRET_KEY }}
          REGISTRY_PASSWORD_FALLBACK: ${{ secrets.SCALEWAY_API_KEY }}
        run: |
          REGISTRY_NAMESPACE="${REGISTRY_NAMESPACE:-assistant-rh}"
          REGISTRY_PASSWORD="${REGISTRY_PASSWORD:-${REGISTRY_PASSWORD_FALLBACK:-}}"
          if [ -z "${REGISTRY_PASSWORD}" ]; then
            echo "Missing Scaleway registry secret: define SCW_SECRET_KEY or SCALEWAY_API_KEY." >&2
            exit 1
          fi
          printf '%s' "${REGISTRY_PASSWORD}" | docker login \
            ${{ env.REGISTRY_HOST }}/${REGISTRY_NAMESPACE} \
            -u nologin \
            --password-stdin

      - name: Build and push staging image
        env:
          REGISTRY_NAMESPACE: ${{ vars.SCW_CONTAINER_REGISTRY_NAMESPACE }}
        run: |
          REGISTRY_NAMESPACE="${REGISTRY_NAMESPACE:-assistant-rh}"
          docker buildx build \
            --platform linux/amd64 \
            -f "${{ matrix.dockerfile }}" \
            -t "${{ env.REGISTRY_HOST }}/${REGISTRY_NAMESPACE}/${{ matrix.image }}:${{ env.STAGING_IMAGE_TAG }}" \
            -t "${{ env.REGISTRY_HOST }}/${REGISTRY_NAMESPACE}/${{ matrix.image }}:staging-latest" \
            --push \
            .

  preview-staging:
    name: preview-staging (${{ matrix.name }})
    runs-on: ubuntu-latest
    needs:
      - plan
      - build-staging-images
    if: ${{ needs.plan.result == 'success' && (needs.plan.outputs.mode == 'apply' || needs.plan.outputs.r2 == 'true') && needs.plan.outputs.has_runs == 'true' && needs.build-staging-images.result == 'success' && (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.run_preview_jobs || needs.plan.outputs.r2 == 'true'))) }}
    # > job_timeout R2 (14400s) + marge : --wait doit voir le statut terminal.
    timeout-minutes: 250
    environment: scaleway-staging
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.run_matrix) }}
    env:
      # Staging branch pushes run a complete preview with destructive chunk wipe disabled.
      # Manual dispatch keeps operator controls.
      SERVICE_PUBLIC_SELECTED: ${{ matrix.service_public }}
      # mode=plan (socle #288) neutralise toute mutation, y compris l'ingestion
      # interne du médaillon PDF (via RUN_INGESTION). push et dispatch apply
      # gardent le comportement historique.
      RUN_INGESTION: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.run_ingestion && inputs.mode == 'apply') || false }}
      WIPE_EXISTING_CHUNKS: ${{ github.event_name == 'workflow_dispatch' && inputs.wipe_existing_chunks || false }}
      RUN_EMBEDDINGS: ${{ (github.event_name == 'push' && needs.plan.outputs.run_embeddings == 'true') || (github.event_name == 'workflow_dispatch' && inputs.mode == 'apply' && (inputs.run_embeddings || inputs.source == 'embeddings' || inputs.source == 'matte' || inputs.source == 'mi' || inputs.source == 'masa' || inputs.source == 'mso')) || false }}
      EMBEDDING_SOURCE: ${{ matrix.embedding_source }}
      EMBEDDING_ONLY_COLUMN: ${{ github.event_name == 'workflow_dispatch' && inputs.embedding_only_column || '' }}
      R2_REVIEWED_CACHE: ${{ github.event_name == 'workflow_dispatch' && inputs.r2_reviewed_cache || false }}
      R2_CACHE_SOURCE_ENV: ${{ github.event_name == 'workflow_dispatch' && inputs.r2_cache_source_env || 'staging' }}
      R2_LIMIT: ${{ github.event_name == 'workflow_dispatch' && inputs.r2_limit || 0 }}
      R2_ALLOW_CACHE_MISSES: ${{ github.event_name == 'workflow_dispatch' && inputs.r2_allow_cache_misses || false }}
      DELTA_MODE: ${{ github.event_name == 'push' || inputs.delta }}
      SERVICE_PUBLIC_FICHE_CONFIG: ${{ github.event_name == 'workflow_dispatch' && inputs.service_public_fiche_config || 'config/service_public_fiches.json' }}
      LEGIFRANCE_ARTICLE_IDS_JSON: ${{ github.event_name == 'workflow_dispatch' && inputs.legifrance_article_ids_json || 'config/legifrance_article_cids.json' }}
      SCW_DEFAULT_REGION: fr-par
      SCW_DEFAULT_PROJECT_ID: ${{ secrets.SCW_DEFAULT_PROJECT_ID }}
      SCW_DEFAULT_ORGANIZATION_ID: ${{ secrets.SCW_DEFAULT_ORGANIZATION_ID }}
      SCW_CONTAINER_REGISTRY_NAMESPACE: ${{ vars.SCW_CONTAINER_REGISTRY_NAMESPACE }}
      SCW_BUCKET_BRONZE: assistant-rh-bronze
      SCW_BUCKET_SILVER: assistant-rh-silver
      SCW_BUCKET_GOLD: assistant-rh-gold
      SCW_PREFIX_STAGING: staging
      SCW_PREFIX_PROD: prod
      SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
      SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
      SCW_POSTGRES_DSN: ${{ secrets.SCW_POSTGRES_DSN }}
      SCALEWAY_API_KEY: ${{ secrets.SCALEWAY_API_KEY }}
      SCALEWAY_BASE_URL: ${{ vars.SCALEWAY_BASE_URL || 'https://api.scaleway.ai/v1' }}
      GRIST_API_BASE_URL: ${{ vars.GRIST_API_BASE_URL }}
      GRIST_API_KEY: ${{ secrets.GRIST_API_KEY }}
      GRIST_DOC_ID: ${{ vars.GRIST_DOC_ID }}
      GRIST_TABLE_ID: ${{ vars.GRIST_TABLE_ID }}
      ALBERT_API_KEY: ${{ secrets.ALBERT_API_KEY }}
      ALBERT_BASE_URL: ${{ vars.ALBERT_BASE_URL || 'https://albert.api.etalab.gouv.fr/v1' }}
      LEGIFRANCE_CLIENT_ID: ${{ secrets.LEGIFRANCE_CLIENT_ID }}
      LEGIFRANCE_CLIENT_SECRET: ${{ secrets.LEGIFRANCE_CLIENT_SECRET }}
      SCW_BUCKET_SOURCES_PDF: assistant-rh-sources-pdf

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Validate Service-Public wipe/backfill selection
        run: |
          if [[ "${SERVICE_PUBLIC_SELECTED}" == "true" && "${RUN_INGESTION}" == "true" && "${WIPE_EXISTING_CHUNKS}" == "true" ]]; then
            if [[ "${RUN_EMBEDDINGS}" != "true" || ( "${EMBEDDING_SOURCE}" != "service_public" && "${EMBEDDING_SOURCE}" != "all" ) ]]; then
              echo "::error::wipe_existing_chunks deletes Service-Public chunk embeddings. Select run_embeddings=true and embedding_source=service_public or all."
              exit 1
            fi
          fi

      - name: Install Scaleway CLI
        uses: scaleway/action-scw@v0.0.3
        with:
          version: v2.54.0
          save-config: true
          export-config: true
          access-key: ${{ secrets.SCW_ACCESS_KEY }}
          secret-key: ${{ secrets.SCW_SECRET_KEY }}
          default-project-id: ${{ secrets.SCW_DEFAULT_PROJECT_ID }}
          default-organization-id: ${{ secrets.SCW_DEFAULT_ORGANIZATION_ID }}

      - name: Start selected Scaleway staging jobs
        run: |
          python3 .github/scripts/scaleway_data_jobs.py \
            --target-env staging \
            --image-tag "${STAGING_IMAGE_TAG}" \
            --mode "${{ needs.plan.outputs.mode }}" \
            --delta "${DELTA_MODE}" \
            --service-public "${{ matrix.service_public }}" \
            --legifrance "${{ matrix.legifrance }}" \
            --pdf-sources "${{ matrix.pdf_sources }}" \
            --pdf-sources-ministry "${{ matrix.pdf_sources_ministry }}" \
            --embeddings "${{ matrix.embeddings }}" \
            --r2 "${{ matrix.r2 }}" \
            --run-ingestion "${RUN_INGESTION}" \
            --wipe-existing-chunks "${WIPE_EXISTING_CHUNKS}" \
            --run-embeddings "${RUN_EMBEDDINGS}" \
            --embedding-source "${EMBEDDING_SOURCE}" \
            --embedding-only-column "${EMBEDDING_ONLY_COLUMN}" \
            --r2-reviewed-cache "${R2_REVIEWED_CACHE}" \
            --r2-cache-source-env "${R2_CACHE_SOURCE_ENV}" \
            --r2-limit "${R2_LIMIT}" \
            --r2-allow-cache-misses "${R2_ALLOW_CACHE_MISSES}" \
            --service-public-fiche-config "${SERVICE_PUBLIC_FICHE_CONFIG}" \
            --legifrance-article-ids-json "${LEGIFRANCE_ARTICLE_IDS_JSON}" \
            --wait

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.12

      - name: Install Python dependencies
        run: uv sync --frozen

      - name: Run staging data quality gates
        if: ${{ matrix.service_public == true || matrix.legifrance == true }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/data-quality

          QUALITY_SOURCES=()
          if [[ "${{ matrix.service_public }}" == "true" ]]; then
            QUALITY_SOURCES+=(--source service_public)
          fi
          if [[ "${{ matrix.legifrance }}" == "true" ]]; then
            QUALITY_SOURCES+=(--source legifrance)
          fi

          BLOCKING_ARGS=()
          if [[ "${RUN_INGESTION}" == "true" ]]; then
            BLOCKING_ARGS+=(--blocking)
          fi

          uv run --no-sync data-ingestion quality gates \
            --target-env staging \
            "${QUALITY_SOURCES[@]}" \
            "${BLOCKING_ARGS[@]}" \
            --json-output "artifacts/data-quality/staging-${{ matrix.name }}-report.json" \
            --markdown-output "artifacts/data-quality/staging-${{ matrix.name }}-summary.md"

      # Embedding coverage is gated separately by the read-only audit shipped in
      # `data-ingestion embeddings <source> --check-only` (issue #133) so a single
      # implementation owns embedding-coverage logic (see issue #172).
      - name: Run staging embedding coverage gate
        if: ${{ matrix.embeddings == true }}
        env:
          COVERAGE_MIN_PCT: "100"
        run: |
          set -euo pipefail
          mkdir -p artifacts/data-quality

          EMBED_CMDS=()
          case "${EMBEDDING_SOURCE}" in
            all) EMBED_CMDS=(service-public legifrance) ;;
            service_public) EMBED_CMDS=(service-public) ;;
            legifrance) EMBED_CMDS=(legifrance) ;;
            matte) EMBED_CMDS=(matte) ;;
            mi) EMBED_CMDS=(mi) ;;
            masa) EMBED_CMDS=(masa) ;;
            mso) EMBED_CMDS=(mso) ;;
            *) echo "Unknown EMBEDDING_SOURCE='${EMBEDDING_SOURCE}'; skipping embedding gate"; EMBED_CMDS=() ;;
          esac

          ONLY_COLUMN_ARGS=()
          if [[ -n "${EMBEDDING_ONLY_COLUMN}" ]]; then
            ONLY_COLUMN_ARGS+=(--only-column "${EMBEDDING_ONLY_COLUMN}")
          fi

          status=0
          for cmd in "${EMBED_CMDS[@]}"; do
            echo "::group::Embedding coverage gate — ${cmd}"
            if ! uv run --no-sync data-ingestion embeddings "${cmd}" \
                  --check-only --coverage-min-pct "${COVERAGE_MIN_PCT}" \
                  "${ONLY_COLUMN_ARGS[@]}" \
                  > "artifacts/data-quality/staging-embeddings-${cmd}.json"; then
              status=1
            fi
            echo "::endgroup::"
          done
          exit "${status}"

      - name: Summarize staging data quality gates
        if: always()
        run: |
          if [[ -f "artifacts/data-quality/staging-${{ matrix.name }}-summary.md" ]]; then
            cat "artifacts/data-quality/staging-${{ matrix.name }}-summary.md" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload staging data quality reports
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: data-quality-staging-${{ matrix.name }}-${{ github.run_attempt }}
          path: artifacts/data-quality/
          if-no-files-found: ignore
