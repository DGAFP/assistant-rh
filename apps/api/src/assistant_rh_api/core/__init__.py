"""Pure business rules and ports for Assistant RH.

The core package must stay importable without FastAPI, database drivers, or
provider clients. Import concrete modules explicitly from their submodules.
"""
